%h2 All Recipes


%ul
  - @recipes.each do |recipe|
    %li.index-list
      = link_to (image_tag recipe.image, class: 'recipe-thumbs'), recipe_path(recipe)
      %br
      = link_to recipe.name, recipe_path(recipe)
